{
  "name": "orbitalview-wallpaper",
  "private": true,
  "version": "0.1.0",
  "description": "OrbitalView Wallpaper – live NOAA satellite imagery as your Windows desktop background",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "tauri": "tauri dev",
    "tauri:dev": "npm run build && tauri dev -- --no-dev-server",
    "tauri:build": "npm run build && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.1",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
